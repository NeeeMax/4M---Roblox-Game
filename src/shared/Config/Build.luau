--!strict
-- Printed in Output when a playtest starts, so you can see at a glance which code version Studio is running.
-- Bump the label with every change that should be visible in a playtest.

return {
	Label = "2026-09-25 #24: clean top bar (no ! badges, hamburger icon, short money), admin Magnet/Manage/Run/Stack toggles; #23: economy v2 (30 Shibas up to 1e33, rebirth, tutorial start), catch-timing bonks (AIR / PERFECT BONK, landing ring), Bonk Basket + Bonk Intern, island zones with stalls on the outer ring",
}
