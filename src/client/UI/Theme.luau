--!strict
-- Colours and fonts shared by every menu, so the whole UI looks like one game.

return {
	Font = Enum.Font.FredokaOne,
	BodyFont = Enum.Font.GothamBold,

	Background = Color3.fromRGB(24, 22, 36),
	Panel = Color3.fromRGB(36, 33, 54),
	Card = Color3.fromRGB(50, 46, 74),
	CardLight = Color3.fromRGB(66, 61, 96),
	Stroke = Color3.fromRGB(12, 10, 20),

	Text = Color3.fromRGB(255, 255, 255),
	Muted = Color3.fromRGB(175, 170, 200),
	Accent = Color3.fromRGB(255, 190, 40),

	Good = Color3.fromRGB(70, 200, 100),
	Bad = Color3.fromRGB(230, 70, 80),
	Info = Color3.fromRGB(80, 160, 255),
	Disabled = Color3.fromRGB(85, 82, 100),
	Admin = Color3.fromRGB(200, 60, 70),
}
